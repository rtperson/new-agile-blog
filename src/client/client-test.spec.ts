import * as chai from "chai";

const expect = chai.expect;

describe("Nothing", () => {
    it("should run", () => {
        //console.log("hello");
        expect(2).to.equal(2, "2 is not equal");
    });
});
